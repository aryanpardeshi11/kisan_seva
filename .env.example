# CropMind / KisanScan Environment Configuration

# Optional: Groq API Key for LLM chat assistant (falls back to local knowledge base if omitted)
GROQ_API_KEY=

# Optional: OpenWeatherMap API Key for live weather metrics (falls back to district seasonal data if omitted)
OPENWEATHER_API_KEY=
